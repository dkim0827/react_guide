/*
<ESLint && Prettier for better coding>
ESLint = Tool for syntax check
Prettier = Tool for automatic code style cleaning.

at the root directory add
.prettierrc

inside .prettierrc add

{
    "singleQuote": true,
    "semi": true,
    "useTabs": false,
    "tabWidth": 2
}

what each line does.
- rather than doublequote use single qutoe
- always put semicolone
- don't use tab
- rather than tap use 2 spaces for indentation
 */
